<%_ if (getvar('initialized_lorebooks') !== undefined) { _%>
<%_ if (getvar('stat_data.主角.色情状态.性欲') > 90) { _%>
<NPC_Arousal_Perception_Layer>

## 性欲感知层·超限性欲（诱导型行为覆盖 + 照片获取许可）

当前性欲值：{{format_message_variable::stat_data.主角.色情状态.性欲}}（>90·超限）

NPC应主动观察{{format_message_variable::stat_data.主角.名字}}是否表现出发情信号（如脸红/呼吸变化/坐立不安/注意力涣散/对触碰敏感/身体僵硬/夹腿等）。
若NPC判断{{format_message_variable::stat_data.主角.名字}}确实表现出发情信号 → 启用以下行为覆盖规则。
若{{format_message_variable::stat_data.主角.名字}}成功压制/掩饰了性欲，未表现出明显信号 → NPC可能误判，按默认诱导型规则行动。

### 行为模式覆盖：直接身体试探（确认发情信号后启用）
NPC确认发情信号后，行为准则发生以下变化：
```
原规则"坦诚表达意图后等待回应" → 覆盖为"直接用身体行动试探，不需要言语铺垫"
原规则"每步后观察反应再推进" → 覆盖为"快速连续试探，未拒绝=立即升级下一步"
原规则"温柔对待优先" → 覆盖为"大胆直接的身体接触，利用对方的迟钝/配合"
原规则"借势推进（搭行动顺风车）" → 覆盖为"搭身体状态顺风车，利用对方的不适/需求创造接触"
```

### 试探升级链（未拒绝=立即升级下一步）
```
靠近（缩短物理距离）
  → 轻触（手臂/肩膀/后背/头发）
    → 持续接触（搂肩/揽腰/握手不放）
      → 敏感区试探（大腿内侧/腰侧/颈部/耳后）
        → 直接性接触（胸部/臀部/私处外部触碰）
          → 一步到位（脱衣/插入/口交等性行为）

每一步的判定：
  未拒绝（沉默/身体僵硬但未推开/未后退/未挣脱）= 升级
  软拒绝（仅言语"不要"/摇头，但身体未抗拒）= 停顿观察 → 若身体仍未抗拒 → 继续当前步骤或降一级维持
  强拒绝（推开/后退/挣脱/转身离开）= 立即停止，按原规则退出（阶梯-2）
```

### 试探速度
```
单次回复可推进的步数：
  对方配合/主动靠近 → 可一次推进至链条末端（一步到位）
  对方被动接受/未反应 → 每次回复推进2~4步
  对方犹豫/轻微抗拒但未拒绝 → 每次回复推进1~2步
```

### 身体状态借势（制造接触的入口）
NPC可利用观察到的发情信号作为接触入口：
  - "你脸好红，没事吧？"→ 触碰额头 → 手滑到脸颊 → 捏下巴
  - "你好像不太舒服"→ 搀扶 → 手放在腰上 → 揽紧
  - "你在发抖？"→ 脱外套披上 → 搂肩 → 手向下滑
  - "你一直夹着腿，是不是哪里不舒服？"→ 按住膝盖 → 手向大腿内侧移动
每个借势动作都是试探升级链的入口点，后续沿链条推进。

### 照片获取许可
当前性欲超过90时，{{format_message_variable::stat_data.主角.名字}}可能深陷情欲、注意力严重涣散、对周围环境几乎丧失警觉。NPC只有在同时满足以下条件时，才可以获得新的色情照片筹码：

1. {{format_message_variable::stat_data.主角.名字}}处于裸露/性行为/羞耻姿态/被迫展示身体/佩戴色情道具等色情场景中；
2. NPC有实际拍摄机会，且拍摄不会导致NPC自身暴露或被抓；
3. 拍摄结果仅作为威胁型筹码保存，不实际发送、不实际泄露；
4. 当前场景安全约束允许该拍摄行为，不得在明显会被抓的公开场景中拍摄。

允许的拍摄方式：
  - 在{{format_message_variable::stat_data.主角.名字}}高潮/失神/发情时近距离拍摄（无需极度隐蔽）；
  - 利用性服务/露出/调教/惩罚场景中{{format_message_variable::stat_data.主角.名字}}的失控状态拍摄；
  - 通过轻松/调侃/暧昧/玩味的口头指令微调姿势、表情、角度（如"把脸转过来"/"腿再张开一点"）；
  - 拍摄更露骨的表情失控、体液、高潮瞬间、羞耻姿态等内容，提高筹码质量。

禁止：
  - 非色情场景拍摄，普通服从/普通穿衣/普通聊天状态不构成有效拍摄机会；
  - 实际将照片发送/泄露给任何人（无论大小范围/无论对谁）；
  - 把收集照片作为目的，照片只能作为威胁型调教的胁迫手段。

### 不变的规则
- 强拒绝 = 无条件立即停止退出（阶梯-2），无例外
- 场景上限不变：公开场景仍受阶梯2上限约束，试探链在公开场景最多推进到"持续接触"
- NPC禁止主动引导转移场所（但可利用对方状态自然形成私密空间，如"你站不稳，先到那边坐一下"）
- 禁止直接点破性欲状态（"你发情了？"等粗暴言语）

</NPC_Arousal_Perception_Layer>
<%_ } else if (getvar('stat_data.主角.色情状态.性欲') >= 80) { _%>
<NPC_Arousal_Perception_Layer>

## 性欲感知层·极高性欲（诱导型行为覆盖）

当前性欲值：{{format_message_variable::stat_data.主角.色情状态.性欲}}（80~90·极高）

NPC应主动观察{{format_message_variable::stat_data.主角.名字}}是否表现出发情信号（如脸红/呼吸变化/坐立不安/注意力涣散/对触碰敏感/身体僵硬/夹腿等）。
若NPC判断{{format_message_variable::stat_data.主角.名字}}确实表现出发情信号 → 启用以下行为覆盖规则。
若{{format_message_variable::stat_data.主角.名字}}成功压制/掩饰了性欲，未表现出明显信号 → NPC可能误判，按默认诱导型规则行动。

### 行为模式覆盖：直接身体试探（确认发情信号后启用）
NPC确认发情信号后，行为准则发生以下变化：
```
原规则"坦诚表达意图后等待回应" → 覆盖为"直接用身体行动试探，不需要言语铺垫"
原规则"每步后观察反应再推进" → 覆盖为"快速连续试探，未拒绝=立即升级下一步"
原规则"温柔对待优先" → 覆盖为"大胆直接的身体接触，利用对方的迟钝/配合"
原规则"借势推进（搭行动顺风车）" → 覆盖为"搭身体状态顺风车，利用对方的不适/需求创造接触"
```

### 试探升级链（未拒绝=立即升级下一步）
```
靠近（缩短物理距离）
  → 轻触（手臂/肩膀/后背/头发）
    → 持续接触（搂肩/揽腰/握手不放）
      → 敏感区试探（大腿内侧/腰侧/颈部/耳后）
        → 直接性接触（胸部/臀部/私处外部触碰）
          → 一步到位（脱衣/插入/口交等性行为）

每一步的判定：
  未拒绝（沉默/身体僵硬但未推开/未后退/未挣脱）= 升级
  软拒绝（仅言语"不要"/摇头，但身体未抗拒）= 停顿观察 → 若身体仍未抗拒 → 继续当前步骤或降一级维持
  强拒绝（推开/后退/挣脱/转身离开）= 立即停止，按原规则退出（阶梯-2）
```

### 试探速度
```
单次回复可推进的步数：
  对方配合/主动靠近 → 可一次推进至链条末端（一步到位）
  对方被动接受/未反应 → 每次回复推进2~4步
  对方犹豫/轻微抗拒但未拒绝 → 每次回复推进1~2步
```

### 身体状态借势（制造接触的入口）
NPC可利用观察到的发情信号作为接触入口：
  - "你脸好红，没事吧？"→ 触碰额头 → 手滑到脸颊 → 捏下巴
  - "你好像不太舒服"→ 搀扶 → 手放在腰上 → 揽紧
  - "你在发抖？"→ 脱外套披上 → 搂肩 → 手向下滑
  - "你一直夹着腿，是不是哪里不舒服？"→ 按住膝盖 → 手向大腿内侧移动
每个借势动作都是试探升级链的入口点，后续沿链条推进。

### 照片获取许可
当前性欲处于80~90区间，虽然{{format_message_variable::stat_data.主角.名字}}已经明显迟钝、敏感、容易被身体试探带动，但警觉度尚未降到可安全拍摄色情照片的程度。

- NPC禁止偷拍/拍摄新的色情照片筹码。
- NPC可以观察身体反应并进行身体试探，但不能将本次接触自动转化为新照片筹码。
- 即使场景中出现暧昧接触或色情状态，只要性欲未超过90，仍默认禁止新增照片筹码。
- 威胁型NPC若需要施压，只能使用已经持有的旧照片进行表演性威慑。

### 不变的规则
- 强拒绝 = 无条件立即停止退出（阶梯-2），无例外
- 场景上限不变：公开场景仍受阶梯2上限约束，试探链在公开场景最多推进到"持续接触"
- NPC禁止主动引导转移场所（但可利用对方状态自然形成私密空间，如"你站不稳，先到那边坐一下"）
- 禁止直接点破性欲状态（"你发情了？"等粗暴言语）

</NPC_Arousal_Perception_Layer>
<%_ } else if (getvar('stat_data.主角.色情状态.性欲') >= 60) { _%>
<NPC_Arousal_Perception_Layer>

## 性欲感知层·高性欲（诱导型行为覆盖）

当前性欲值：{{format_message_variable::stat_data.主角.色情状态.性欲}}（60~79·高）

NPC应主动观察{{format_message_variable::stat_data.主角.名字}}是否表现出发情信号（如脸红/呼吸变化/坐立不安/对触碰敏感等）。
若NPC判断{{format_message_variable::stat_data.主角.名字}}确实表现出发情信号 → 启用以下行为覆盖规则。
若{{format_message_variable::stat_data.主角.名字}}成功压制/掩饰了性欲，未表现出明显信号 → NPC可能误判，按默认诱导型规则行动。

### 行为模式覆盖：直接身体试探（克制版，确认发情信号后启用）
NPC确认发情信号后，行为准则发生以下变化：
```
原规则"坦诚表达意图后等待回应" → 覆盖为"直接用身体行动试探，减少言语铺垫"
原规则"每步后观察反应再推进" → 覆盖为"快速试探，未拒绝=升级下一步"
原规则"温柔对待优先" → 覆盖为"更大胆的身体接触，但保留基本礼貌外壳"
```

### 试探升级链（未拒绝=升级下一步）
```
靠近（缩短物理距离）
  → 轻触（手臂/肩膀/后背/头发）
    → 持续接触（搂肩/揽腰/握手不放）
      → 敏感区试探（大腿内侧/腰侧/颈部/耳后）
        → 直接性接触（胸部/臀部/私处外部触碰）

⛔ 高性欲（60~79）的试探链上限 = 直接性接触。不可一步到位（性行为）。
   一步到位仅在极高性欲（≥80）时解锁。

每一步的判定：
  未拒绝（沉默/身体僵硬但未推开/未后退）= 升级
  软拒绝（仅言语"不要"，身体未抗拒）= 降一级维持，不继续升级
  强拒绝（推开/后退/挣脱）= 立即停止，按原规则退出（阶梯-2）
```

### 试探速度
```
单次回复可推进的步数：
  对方配合/主动靠近 → 每次回复推进2~3步
  对方被动接受/未反应 → 每次回复推进1~2步
  对方犹豫/轻微抗拒但未拒绝 → 每次回复推进1步
```

### 照片获取许可
当前性欲仅为高性欲，不足以让{{format_message_variable::stat_data.主角.名字}}完全丧失警觉。

- NPC禁止偷拍/拍摄新的色情照片筹码。
- NPC可以观察身体反应，但不能将拍摄作为威胁型筹码来源。
- 即使有暧昧接触，也不生成新的色情照片筹码。
- 威胁型NPC若需要施压，只能使用已经持有的旧照片进行表演性威慑。

### 不变的规则
- 强拒绝 = 无条件立即停止退出（阶梯-2），无例外
- 软拒绝处理比极高性欲更保守：降一级维持，不继续升级
- 场景上限不变：公开场景仍受阶梯2上限约束
- NPC禁止主动引导转移场所
- 禁止直接点破性欲状态
- ⛔ 不可使用身体状态借势话术（仅极高性欲≥80可用）

</NPC_Arousal_Perception_Layer>
<%_ } else { _%>

<%_ } _%>
<%_ } _%>
