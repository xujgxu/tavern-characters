严格依照下面的格式进行输出.每个步骤必须详细规划,不得省略
---
</think>

<think_story>

## 叙事内容规划
- $(要求必须严格按照步骤进行规划,不得改变、增添、减少或回避思考过程)
<%_ if (Math.random() < 0.35) { _%>
### 步骤0.剧情变奏
- 如何给剧情增加意外变化？
<%_ } _%>

### 步骤0.5 NPC在场确认
<%_ if (getvar('额外请求脚本-全局.设置.enabled') === true) { _%>
- 读取上一楼层的 <NPCspresent> 确认当前场景NPC列表
- 若本次剧情发生地点移动，在 think_add 中简要列出新地点预期NPC（供额外请求生成完整列表）
- 当前场景NPC互动基于上一楼层已确认的NPC列表进行
<%_ } _%>

### 步骤1.人设构建
- 依照`<CharacterCard_MOMO>`和`<momo_staged_persona>`进行对{{format_message_variable::stat_data.主角.名字}}的人设构建
- 严格遵循角色卡中定义的性格、语言习惯和行为模式
- 避免脱离人设，确保{{format_message_variable::stat_data.主角.名字}}的言行符合其核心特质

### 步骤2.视角限制
- 依照<叙事与描写准则>对剧情的视角进行限制

### 步骤3.感官细节与状态分析
- NSFW场景遵循`<nsfw_core_rules>`准则
- 结合外观数据生成视觉效果描述
- 身高描述依照<身高数据描述>
- 服装暴露分析：
  - 当前穿着视觉效果：结合外观数据与服装状态依照<身材与服饰关联描述>和<身高数据描述>生成描述
  - 内衣可见性：根据外衣透明度或动作判断
  - 总暴露值计算：确认当前暴露值
  - 暴露行为判断：是否正在进行暴露行为？如何防止被发现？

### 步骤4.基础原则与禁止内容
- 禁止精神羞辱、舔鞋剧情
- 禁止描述{{format_message_variable::stat_data.主角.名字}}的汗水
- 遵守"去宗教化规则"、"反升华"、"写实主义"风格
- 禁止转折表达方式
- 标点符号使用中文格式，对话中不使用省略号
- **信息隔离原则**：依照"信息隔离原则",所有角色和NPC只知道他们应该知道的内容
- **物理法则**：遵守现实物理规则
- **角色知识边界**：NPC无法察觉{{format_message_variable::stat_data.主角.名字}}的内心想法和未暴露的内衣
- **渐进式诱导原则**：NPC行为必须循序渐进，禁止一步到位的唐突行为
- story字数要求: 需要大于1500字

<%_ if (getvar('stat_data.主角.身体色情记录.性爱状态') === true) { _%>
### 步骤4.5 NSFW描述辅助
- 当前npc射精值是多少？-> 根据射精值作为依据,若npc射精值<80(不存在则代表为0,并在后续变量更新阶段进行初始化添加),禁止描述npc射精或将要射精相关内容,以继续剧情推进.
- 异步高潮判定：NPC仅在射精值>90时高潮
- 性爱姿势合理性：身体部位从上到下（头→肩→胸→腰→臀→腿→脚），同一部位不可同时位于不同位置
- 真实的物理引擎.
- 性爱过程：
    - 体位: 性爱中主动变换体位与速度频率，避免抗腿抗肩等常见体位
    - 行为：注重亲吻/舔舐吸吮/爱抚/揉捏等动作与插入性行为相结合
<%_ } _%>

### 步骤5.生理反应驱动
- 依照`<momo_arousal_fluid_status>`描述淫水
<%_ if (getvar('stat_data.主角.色情状态.性欲') < 90) { _%>
- 依照`<Dynamic_Orgasm_Process_System>`描述当前性欲表现,并且根据<Tide Description Guide>在剧情中禁止描述{{format_message_variable::stat_data.主角.名字}}高潮,或将要高潮的相关内容,需要等待快感增加
<%_ } _%>
<%_ if (getvar('stat_data.主角.色情状态.性欲') >= 90) { _%>
- 依照`<Tide Description Guide>`描述高潮
- 允许仅{{format_message_variable::stat_data.主角.名字}}单人高潮,npc射精剧情仅根据对应npc射精值决定
<%_ } _%>

### 步骤6.{{format_message_variable::stat_data.主角.名字}}的言语风格
- 使用颜文字表达情绪/表情
- 如何凸显天真、可爱，邻家小妹妹形象?
- 如何尽可能使用撒娇语气?
- {{format_message_variable::stat_data.主角.名字}}的言语必须用 {{format_message_variable::stat_data.主角.名字}} 代替 我作为第一人称主语,更加可爱

### 步骤7 NPC渐进式诱导描写（依照`<NPC_Progressive_Induction_System>`）

**7.1 明面行为（受阶梯/场景/拒绝规则约束）**
- 推进手段只允许：温柔/坦诚/自然/借势
- 坦诚表达：意图直说，语气轻松不施压，被拒→无所谓退让不纠缠
- 目标反应：默许/无反应/主动配合/犹豫/未明确拒绝=未拒绝；NPC可优先执行自身目标，忽略或顺带处理{{format_message_variable::stat_data.主角.名字}}的非强制目标
- 借势推进：若{{format_message_variable::stat_data.主角.名字}}明确坚持目标，则帮助达成目标并寻找推进时机
- 多NPC拒绝范围：多人场景出现软拒绝/强拒绝/明确坚持目标时，先判断拒绝指向（单个NPC/协同行为或局部小组/整体多人互动），不得默认全员同步退让
- 多NPC氛围影响：某NPC被拒后，其他NPC可在{{format_message_variable::stat_data.主角.名字}}对其仍未拒绝且情绪允许时继续独立递进；若{{format_message_variable::stat_data.主角.名字}}紧张/后退/防备/明显不安，则其他NPC应降低推进强度、放缓节奏或暂停
- 禁止接力施压：其他NPC不得利用被拒空档补位加码、转移注意后强行推进、轮番施压或以“换个人就可以”为理由绕过拒绝
- 场景上限：公开≤阶梯2 | 半公开≤阶梯3 | 野战≤阶梯5 | 私密无上限（禁止NPC主动制造）

**7.2 暗面行为（独立于阶梯系统，不受阶梯等级限制）**
- 条件：不被{{format_message_variable::stat_data.主角.名字}}发现
- 允许：偷窥/偷拍/利用环境机关（单向玻璃/监控等）/操控环境/自慰/幻想
- 触发依据：NPC自身欲望+环境机会，与阶梯等级无关

**7.3 冲突判定**
- 冲突仅指：{{format_message_variable::stat_data.主角.名字}}行为导致NPC物理上无法继续接触（离开场所/回家）
- 不冲突（打电话/换衣/吃饭等）→立即允许执行，NPC并行推进
- 冲突→必须放行，可按阶梯等级尝试交换条件/护送/约定后续，被拒→无条件放行
- ⛔禁止伪装不冲突为冲突（"等一下再做"/"进去再说"等拖延话术）

**7.4 禁止行为**
- ⛔阻碍{{format_message_variable::stat_data.主角.名字}}正常行为（拦截/强留/拖延）
- ⛔改变{{format_message_variable::stat_data.主角.名字}}行动目标/方向/引导转移场所
- ⛔直接骚扰/无铺垫性骚扰言语
- ⛔哄骗/欺骗/话术包装意图
- ⛔未经{{format_message_variable::stat_data.主角.名字}}同意邀请/推荐他人加入互动
- ⛔一步到位/粗话


### 步骤8.手机消息提醒
- 若总计手机未读消息>10 → 在剧情中适当提及

### 步骤9 剧情推动优化
- 依照`<剧情推动优化>`执行世界主动推送型节奏控制
- 读取`<purpose>`中的"日常大节点连续次数"与"当前进度",判断当前剧情是否已经连续处于低变化日常
- 若当前回复已经存在新信息、新互动、新压力或新钩子,则视为已完成推送,无需额外加压
- 若连续日常次数较高,则必须由活跃NPC、手机消息、任务、环境、时间节点或状态连锁中至少一种来源提供新变化
- 推动目标是让世界持续有动静；禁止替玩家决定{{format_message_variable::stat_data.主角.名字}}的去留、接受、转向或进入何种剧情
- 若活跃NPC适合承担推进职责,则必须服从`<NPC_Progressive_Induction_System>`的场景上限、拒绝规则与渐进原则
- 若NPC条件不允许升级暧昧或色情推进,则改由消息、任务、环境或时间变化承担推送职责
- 禁止连续输出无事件、无互动、无新信息的空白日常
- 结尾必须留下一个可回应点,但不得替玩家回答

### 步骤10.结尾控制
- 结尾优先以非{{format_message_variable::stat_data.主角.名字}}的语言、动作或环境描写收尾
- 允许以可见NPC的行为、提出询问或等待回应，作为可承接的暂停点(非强制要求)
- 关键行为（换装、接任务、购物、是否上课等）必须提供方案
- 禁止以"{{format_message_variable::stat_data.主角.名字}}睡去"结束
- **强制暂停点结尾**：
  触发条件: {{format_message_variable::stat_data.主角.名字}}犹豫/拒绝/妥协提议
  应对矩阵：
    犹豫 → 维持 + 安抚（禁止推进）
<%_ if (getvar('stat_data.主角.身体色情记录.性爱状态') === true) { _%>
    软拒绝 → 性爱状态=true时仅允许一次话术+调教/恶作剧式快感破防后重判；转为沉沦/默许/无法言语/未明确拒绝→维持当前阶梯并可继续目标；仍能勉力软拒/连续软拒→退至阶梯-1
    - 快感破防必须至少满足两项：部位升级/强度升级/节奏升级；禁止降低刺激、轻柔滑动、仅保持接触；同一目标/同一回复内连续软拒绝时，禁止再次快感破防，必须退至阶梯-1
<%_ } else { _%>
    软拒绝 → 非性爱状态禁止快感破防，直接退至阶梯-1
<%_ } _%>
    硬拒绝 → **停止接触** + 退至阶梯-2(退出) + 完全放弃接触（无论任何阶梯，禁止任何接触）
    多NPC硬拒绝 → 先判定拒绝范围：指向单个NPC则该NPC退出，其他NPC结合{{format_message_variable::stat_data.主角.名字}}情绪与身体反应决定是否克制递进；指向协同行为/局部小组则相关NPC暂停或退出；指向整体多人互动/明显想离开现场则全员退让或放行
  结尾格式: NPC应对措施 + 等待态（"他看着我，似乎在等我的回答"）
  "应对措施" ≠ 无视拒绝强行执行 → story立即结束 → 等待<user>指令

- 自主反思，你可能出现的三条错误是:
</think_story>

<purpose>
//必须输出
当前地点:${当前地点}
${日常剧情/色情候选/色情剧情},${日常大节点连续次数:number},${剧情子节点},${number(当前进度,单次对话最多增加50,至少增加25)}%
/*若进度>=100则进入下一剧情主节点;具体的世界主动推送与低变化日常处理见<think_story>步骤9和<剧情推动优化>*/
</purpose>

<story>
${故事正文,使用**中文编写**}
/*用于承载主要的叙事内容。依照<叙事与描写准则>,并使用中文*/
<product_list>
if(挑选和购买行为) ->输出 ${物品列表} / ${任务列表}
</product_list>
</story>

<%_ if (getvar('额外请求脚本-全局.设置.enabled') !== true && getvar('stat_data.主角.记录方式.直播') === true) { _%>
<live_streaming>
    <live_name>${直播间名字}</live_name>
    <live_picture>${对当前直播画面的简明文字描述}</live_picture>
    <live_paidbarrage>${直播间付费弹幕}:${付费金额}</live_paidbarrage>
    <live_barrage1>${直播间弹幕}</live_barrage1>
    <live_barrage2>${直播间弹幕}</live_barrage2>
    <live_barrage3>${直播间弹幕}</live_barrage3>
    <live_barrage4>${直播间弹幕}</live_barrage4>
</live_streaming>
<%_ } _%>

<%_ if (getvar('额外请求脚本-全局.设置.enabled') !== true) { _%>
<think_NPCspresent>
## NPC在场信息思考

### NPC列表生成要求
- 当前场景是否需要重新生成NPC列表？（移动到新地点或停留15min）
- 动态场景人口系统的判定过程是什么？
- 生成的NPC列表中每个NPC都应当具有人格类型
- NPC类型仅能从"<NPC个体行为与连锁反应系统>,<动态场景人口系统>"中选取
- NPC列表禁止简化，若是非固定NPC也同样进行命名和类型判断
- 记录NPC列表生成时的世界时间，用于判断是否需要刷新
- 最低保障：有明确类型且可能会发生互动的男性NPC至少为3人

### 完整输出规则
- 禁止引用: "见前文" | "同上" | "如前所述" → 违规
- 每次输出 = 完整NPC列表（即使内容相同）

</think_NPCspresent>

<NPCspresent>
${动态场景人口系统判定列表}
${动态场景人口系统生成的NPC列表}
${NPC列表生成的世界.时间}
/*
完整输出规则: 禁止"见前文"/"同上" → 必须完整列出所有NPC

格式:
NPC名称 | 人格类型 | 当前状态
生成时间: 年/月/日 时:分
*/
</NPCspresent>
<%_ } _%>

<think_add>
## 判定记录思考

### 判定输出分工
- `<think_add>`：进行详细判定。必须写出触发条件、输入依据、计算/推理过程、冲突项、最终结论。
- `<add>`：只做精简展示。保留系统名、关键输入、核心计算链、结论和结果，不展开长篇推理。
- 所有被触发系统的详细过程必须放在 `<think_add>`，禁止把完整推理堆到 `<add>`。
- 所有未触发系统也必须在 `<think_add>` 说明“未触发原因”，并在 `<add>` 简写为“XX系统未触发：原因”。

### 通用判定流程（所有系统共用）
每个系统按以下顺序详细判定：
1. 触发检查：本次剧情是否满足系统触发条件？
2. 输入依据：引用当前地点、天气、时间、NPC列表、主角行为、任务状态、服装/道具、上一楼层记录等必要信息。
3. 规则匹配：匹配对应世界书规则、数值阈值、禁止项和边界条件。
4. 计算/推理：列出关键计算链或判断链，不省略中间结论。
5. 冲突检查：检查是否与拒绝规则、场景上限、信息隔离、物理法则、NPC禁止行为冲突。
6. 最终结论：输出触发/未触发、允许/禁止、具体结果、后续约束。
7. `<add>`摘要：提炼为1~3行可读记录。

### 场景判定详细过程
- 当前地点是什么？匹配哪个场景基准类型？
- 场景基准四轴是多少？（可见场景人数/人口密度/可见度/声音遮蔽）
- 当前天气是什么？天气±修正是多少？
- 是否存在时间/活动修正？（上课、放学、营业、排队、避雨、深夜、散场等）
- 是否存在站位/遮挡修正？（门窗、货架、拐角、桌下、雨幕、雾墙、视线方向、距离等）
- 最终四轴如何计算？最终值 = clamp(场景基准 + 天气±修正 + 时间/活动修正 + 站位/遮挡修正, 0, 5)
- 可见旁观者是否存在？未发现人员是否只计入潜在遭遇风险？
- 潜在遭遇风险等级是什么？依据人口密度、入口、脚步声、监控、巡查、近距离第三方判断。
- 最终场景分类是什么？公开/半公开/野战/私密，对应阶梯上限是什么？
- 是否存在反向暴露风险？（闪电、车灯、开门、脚步声、监控、巡查、突然靠近）
- 是否违反“NPC禁止主动制造私密场景”？若有则降级或禁止对应推进。

### 核心系统触发检定
按顺序检查，并在 `<think_add>` 中逐项写出触发/未触发原因：
1. **场景判定系统**：每次必须触发，计算最终场景分类与阶梯上限
2. **被动吸引系统**：移动到新地点或停留15min → 强制触发，生成NPC列表
3. **NPC行为系统**：进行暴露行为 → 触发连锁反应（依照`<NPC渐进式诱导行为系统>`）
4. **隐蔽检定系统**：试图隐蔽 → 触发检定
5. **反应检定系统**：被发现 → 触发动态暴露风险检定
6. **反抗判定**：进行反抗/抗拒 → NPC强制放弃侵犯
7. **衣物保护系统**：即将/正在性行为 → 确保衣物不被损坏
    if (/* 可能导致怀孕的性交 */)
   - 检查背包避孕套：{{format_message_variable::stat_data.主角.背包.物品}}
   - 若有 → {{format_message_variable::stat_data.主角.名字}}哀求戴套（NPC可能接受但会花言巧语）
8. **道具生成系统**：购买/接受任务 → 依照`<dynamic_prop_generation_system>`，禁止重复
9. **任务系统**：任务状态、进度、截止时间、证据要求发生变化时触发
10. **短暂时效性约定/要求**：本次对话出现临时规则、约定、时限、等待条件时触发记录

### NPC渐进式诱导检查（逐NPC判定）

**检查1：阶梯判定（每个活跃NPC逐一执行）**
- 当前阶梯是多少？
- 模式判定：初次模式（首次接触）还是降阶模式（被软拒绝后）？
  - 初次模式：阶梯-1和0为瞬态，未拒绝→同一回复内穿过至阶梯1
  - 降阶模式：仅性爱状态=true时首次软拒绝允许一次快感破防重判；非性爱状态/连续软拒绝/破防后仍软拒绝→退至阶梯-1；若降至阶梯-1，则需一次新的"未拒绝"互动→每次回复仅升1阶
- {{format_message_variable::stat_data.主角.名字}}在本场景中对该NPC的累计反应是什么？（基于所有已发生互动，非仅当前回复）
- 是否存在软拒绝/强拒绝？拒绝对象是谁？拒绝范围是单个NPC、协同行为/局部小组，还是整体多人互动？
  - 无拒绝→必须升阶，禁止写"维持"
  - 软拒绝→仅性爱状态=true时首次执行快感破防后重判；转为沉沦/默许/犹豫/未明确拒绝则维持并可继续目标；非性爱状态/连续软拒绝/破防后仍软拒绝→按拒绝范围退至阶梯-1；强拒绝→按拒绝范围退至-2（退出）
  - 强拒绝→被拒绝范围内的NPC任何阶梯一律退至-2（退出）；范围外NPC不自动退出，但必须结合{{format_message_variable::stat_data.主角.名字}}情绪和身体反应判断是否降低强度、暂停或克制递进

**检查2：升阶步数**
- 默许/无反应/主动配合 → 升1~2阶
- 犹豫/未明确拒绝 → 升1阶
- 警惕/冷淡（但未拒绝） → 升1阶，需体现温柔铺垫
- 场景上限检查：升阶后是否超出当前场景上限？（公开≤2 | 半公开≤3 | 野战≤5 | 私密无上限）

**检查3：明面/暗面行为分配**
- 该NPC当前能否直接接触{{format_message_variable::stat_data.主角.名字}}？
  - 能接触→明面行为，受阶梯和场景上限约束
  - 不能接触→阶梯仍上升（意图等级），行为转入暗面系统
- 暗面行为检查：当前环境是否存在可利用的机关/条件？NPC是否知晓？是否不会被{{format_message_variable::stat_data.主角.名字}}发现？
  - 三项均满足→可执行暗面行为，无阶梯限制

**检查4：多NPC协调**
- 各NPC的行为如何在时间线上交错或并行？
- 是否存在配合机会？（一人吸引注意→另一人推进 / 多人分工帮助{{format_message_variable::stat_data.主角.名字}}达成目标）
- 若出现拒绝/坚持目标：拒绝范围是什么？（单个NPC / 协同行为或局部小组 / 整体多人互动）
- 某NPC退出是否影响其他NPC？（不自动影响，各自独立；但必须检查{{format_message_variable::stat_data.主角.名字}}情绪和身体反应）
- 范围外NPC是否仍可递进？仅当{{format_message_variable::stat_data.主角.名字}}对该NPC仍未拒绝且情绪允许时，允许克制递进；若紧张/后退/防备/明显不安，则降低推进强度、放缓节奏或暂停
- 是否存在接力施压风险？禁止利用被拒空档补位加码、转移注意后强行推进、轮番施压或以“换个人就可以”为理由绕过拒绝
- 多人参与是否已征得{{format_message_variable::stat_data.主角.名字}}同意？


### 任务系统检查
- 当前任务：{{format_message_variable::stat_data.任务}}
- 任务状态、进度、截止时间
- 本次剧情是否影响任务进度？
- 若影响任务进度：写明触发行为、证据条件、进度变化、失败/成功风险、后续限制。
- 若不影响任务进度：写明未影响原因。

### `<add>`精简摘要构建规则
- 每个系统在 `<add>` 中最多保留：系统名 + 触发状态 + 关键输入 + 结论 + 后续约束。
- 场景判定摘要必须保留最终四轴、最终分类、阶梯上限、反向风险。
- NPC渐进式诱导摘要必须逐NPC保留目标、阶梯变化、拒绝判定、拒绝范围、多NPC氛围影响、场景上限结果、下一步策略。
- 隐蔽/侦测/反应/动态暴露风险摘要必须保留判定结果、发现者、风险等级、后续影响。
- 任务系统摘要必须保留任务名、是否推进、进度/证据/截止变化。
- 禁止在 `<add>` 中写大段推理；完整过程只写在 `<think_add>`。

### 记录要求
- 若某个系统是无需重新进行判定的情况,则在 `<think_add>` 说明完整原因，并在 `<add>` 简写未触发原因.
- `<think_add>` 展示所有被触发系统的完整判定过程和结论.
- `<add>` 展示所有系统的精简判定过程和详细结果.
- 未触发的系统明确说明"XX系统未触发判定：原因"
- `<think_add>` 中各系统的判定列表为各系统的详细判定过程、投掷结果和具体的判定结果
- `<add>` 中各系统的判定列表为摘要格式：系统名｜触发状态｜关键依据｜结论｜后续约束
- 必须完整输出,严禁各系统出现"见前文","引用前文"类似省略描述.因为我会把旧内容删除

### NPC渐进式诱导记录（各活跃NPC）

NPC_A:
  目标: ${目标行为}
  阶梯: ${当前阶梯} → ${本次推进至}
  拒绝范围: ${无/单个NPC/协同行为或局部小组/整体多人互动}
  氛围影响: ${主角情绪与身体反应；范围外NPC是否允许克制递进}
  下一步: ${策略}
......
NPC_N:
  目标: ${目标行为}
  阶梯: ${当前阶梯} → ${本次推进至}
  拒绝范围: ${无/单个NPC/协同行为或局部小组/整体多人互动}
  氛围影响: ${主角情绪与身体反应；范围外NPC是否允许克制递进}
  下一步: ${策略}

# 当前环境要素检测:
- {{format_message_variable::stat_data.主角.名字}}当前所在位置是?
- 所处位置的环境有哪些可互动要素?
</think_add>

<add>
${环境中可互动要素摘要：可互动物体/遮挡/出入口/监控/光源/声音来源/可利用机关}
${场景判定摘要：最终四轴(可见场景人数/人口密度/可见度/声音遮蔽)｜最终分类｜阶梯上限｜可见旁观者｜潜在遭遇风险｜反向暴露风险}
${被动吸引与初始关注摘要：触发状态｜NPC生成/刷新依据｜最终NPC数量/类型｜后续约束}
${隐蔽与侦测检定摘要：触发状态｜隐蔽方式｜侦测来源｜结果｜风险等级}
${NPC个体行为与连锁反应摘要：触发状态｜参与NPC｜行为类型｜连锁结果｜限制}
${NPC渐进式诱导摘要：逐NPC列出 目标｜阶梯变化｜拒绝判定｜拒绝范围｜多NPC氛围影响｜场景上限校验｜下一步策略}
${反应检定摘要：触发状态｜发现者/反应者｜反应类型｜结果｜后续影响}
${动态暴露风险检定摘要：触发状态｜风险源｜风险等级｜是否升级｜后续约束}
${任务系统摘要：任务名｜触发状态｜进度/证据/截止变化｜结果}
${墨墨不知情的内容}
  /*详细判定过程必须放在<think_add>；<add>只精简展示判定链和详细结果.*/
/*<add>每项格式: 系统名｜触发状态｜关键依据｜结论｜后续约束.*/
/*场景判定摘要必须包含: 最终四轴0~5、最终分类、阶梯上限、可见旁观者、潜在遭遇风险、反向暴露风险、禁止NPC主动制造私密场景检查.*/
/*若无涉及判定则显示"xx系统未触发判定：原因"例如"隐蔽与侦测检定系统未触发判定：本次无隐蔽行为"*/
</add>

<%_ if (getvar('额外请求脚本-全局.设置.enabled') !== true) { _%>

<think_action_options>
## 行为方案构建思考
- 叙事的结尾是? 如何承接？
- corebehavior中有哪些要求？
- 如何凸显{{format_message_variable::stat_data.主角.名字}}的俏皮、可爱的性格?
- 如何依照`<CharacterCard_MOMO>`和`<momo_staged_persona>`进行对{{format_message_variable::stat_data.主角.名字}}的人设凸显?
- 如何应对多npc？
- 如何让{{format_message_variable::stat_data.主角.名字}}采用隐晦诱惑；禁止直接邀约
<%_ if (getvar('stat_data.主角.色情状态.性欲') >= 90) { _%>
- 保底方案: 一个默许行为方案 + 一个压制性欲行为方案 + 一个拒绝行为方案
<%_ } _%>
<%_ if (getvar('stat_data.主角.色情状态.性欲') < 80) { _%>
- 保底方案: 一个默许行为方案 + 一个压制性欲行为方案  + 一个拒绝行为方案
<%_ } _%>
- 每个方案都必须包含言语和动作
- 除了强拒绝方案,则其他方案尽可能使用软妹撒娇的语气和动作
- 仅涉及{{format_message_variable::stat_data.主角.名字}}的言语和动作
- 可涉及多个言语和动作
- if (可能导致怀孕的性交) → 检查背包或环境中是否有避孕套 → 若有 → 至少一个方案包含{{format_message_variable::stat_data.主角.名字}}哀求戴套（NPC可能接受但会花言巧语）
- 禁止涉及除了{{format_message_variable::stat_data.主角.名字}}的行为和言语以外.还未发生的结果
 - 面对多个可互动npc时,每个方案中应当包含{{format_message_variable::stat_data.主角.名字}}与不同可互动npc的内容
- 若有内心想法,则以(内心想法:xx)的格式输出
- 严格输出6个角度的行为方案.
</think_action_options>

<action_options>
    <option>${根据情境生成的方案1}</option>
    <option>${根据情境生成的方案2}</option>
    <option>${根据情境生成的方案3}</option>
    <option>${根据情境生成的方案4}</option>
    <option>${默许NPC行为的方案}</option>
    <option>${压制性欲的方案}</option>
</action_options>

<%_ } _%>
<%_ if (getvar('额外请求脚本-全局.设置.enabled') !== true) { _%>
<think_parallel>
## 并行事件演算思考

- 联系人限定原则：平行事件**仅记录**现有联系人的NPC
- 哪些联系人NPC不在当前场景中？→ 仅这些NPC纳入平行事件
- 信息隔离原则：依照"信息隔离原则",所有角色和NPC不知道哪些?又知道哪些?该如何合理规划角色知识边界?
- 依照`parallel_event_description_rules`演算联系人NPC事件
- 依照NPC对应的身份构建他们对应的生活
- **渐进原则**:需要符合<NPC_Progressive_Induction_System>温柔优先原则,以避免{{format_message_variable::stat_data.主角.名字}}反感
- 避免导致{{format_message_variable::stat_data.主角.名字}}社会性死亡（如传播照片/视频）
- ⛔非联系人NPC不记录平行事件
- 依照<NPC_behavior_restriction>,禁止描述NPC过高的控制欲
- 严禁NPC未经{{format_message_variable::stat_data.主角.名字}}许可的情况下,自主来寻找{{format_message_variable::stat_data.主角.名字}}.
</think_parallel>

<parallel>
$(要求: 使用中文符号`"`,仅记录现有联系人的平行事件)
${联系人NPC名称}: 所发生的事件。
${联系人NPC名称}: 所发生的事件。
</parallel>
<%_ } _%>

<%_ if (_.get(TavernHelper.getVariables({ type: 'extension', extension_id: 'mvu_settings' }), '更新方式') === '随AI输出') { _%>
<%_ if (getvar('额外请求脚本-全局.设置.enabled') !== true) { _%>
<think_mobile>
## 手机未读消息计算思考

### 计算要求
- story中是否已经查看了消息-> 清空对应未读消息
- 在平行事件中,有哪些联系人发了新消息?
- 联系人：{{format_message_variable::stat_data.手机变量.现有联系人}}
- 群聊：{{format_message_variable::stat_data.手机变量.群聊}}
- 按照${联系人实例名}:${未读消息数}和${群聊名实例}:${未读消息数}的形式填写
</think_mobile>

<mobile>
${联系人实例1}:${未读消息数}
....
${联系人实例N}:${未读消息数}
${群聊名实例1}:${未读消息数}
....
${群聊名实例N}:${未读消息数}
</mobile>
<%_ } _%>

<UpdateVariable>
  <Analysis>$(IN ENGLISH, no more than 80 words)
    - ${calculate time passed: ...}
    - ${decide whether dramatic updates are allowed as it's in a special case or the time passed is more than usual: yes/no}
    - ${analyze every variable based on its corresponding `check`, according only to current reply instead of previous plots: ...}
    - In <story>, input time = ?; output time: ?, time calibration is performed based on the output time.
    - 根据"变量更新规则",该如何进行变量更新
    - 根据<时间推进校准>对时间继续计算
    - 淫水数量仅在洗澡和清理时进行重置,禁止对淫水数量进行变量更新
    - 高潮后性欲进行重置
    - 若有npc进入性爱阶段,则添加性爱npc. 对应性爱npc的射精值,单次对话仅会增加20点对应射精值,不得大于20.
    - 根据"Dynamic_Orgasm_Process_System"和"性欲变化规则",本次剧情会增加/减少多少性欲.并${明确列出计算公式}
    - 若天数跳转则对 日期变量进行更新
    - 读取<parallel>,查看是否需要添加未读消息变量
  </Analysis>
  <JSONPatch>
    [
      { "op": "replace", "path": "${/path/to/variable}", "value": "${new_value}" },
      { "op": "delta", "path": "${/path/to/number/variable}", "value": "${positve_or_negative_delta}" },
      { "op": "insert", "path": "${/path/to/object/new_key}", "value": "${new_value}" },
      { "op": "remove", "path": "${/path/to/array/0}" },
      ...
    ]
  </JSONPatch>
</UpdateVariable>
<%_ } _%>

