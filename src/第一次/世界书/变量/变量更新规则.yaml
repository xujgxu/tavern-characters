---
变量更新规则:
  世界:
    时间:
      format: YYYY-MM-DD HH:MM
      check:
        - 参考「时间推进机制」更新
  主角:
    饱食度:
      type: number
      range: 0~100
      check:
        - 参考「饱食度机制」更新
    精神值:
      type: number
      range: 0~100
      check:
        - 参考「精神值机制」更新
    金钱:
      type: number
      check:
        - 参考「金钱机制」更新
    心情值:
      type: number
      range: 0~100
      check:
        - 参考「心情值机制」更新
    性欲值:
      type: number
      range: 0~100
      check:
        - 参考「性欲值与快感值」更新
    快感值:
      type: number
      range: 0~100
      check:
        - 参考「性欲值与快感值」更新
    整体色情值:
      type: number
      range: 0~100
      check:
        - 参考「服装机制」更新
